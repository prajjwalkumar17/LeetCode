package LinkedList;

public class ReverseRecursive {

//    private static Node reverseRECList(Node node) {
//        if(node==null || node.next==null)return node;
//        Node head=node;
//        Node lastNode=reverseRECList(head.next);
//        head.next.next=head;
//        head.next=null;
//        return lastNode;
//    }

//    TODO Method2
//    private static Node reverseRECList(Node curr,Node prev) {
//        if(curr==null) return prev;
//        Node next=curr.next;
//        curr.next=prev;
//        return reverseRECList(next,curr);
//    }
}
