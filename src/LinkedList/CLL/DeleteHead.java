package LinkedList.CLL;

public class DeleteHead {

//    private static Main.Node deleteHeadEFF(Main.Node head) {
//        if(head==null||head.next==head)return null;
//        head.data=head.next.data;
//        head.next=head.next.next;
//        return head;
//    }
//

//    private static Main.Node deleteHeadNAIVE(Main.Node head) {
//        if(head==null||head.next==head)return null;
//        Main.Node temp=head;
//        while(temp.next!=head)
//            temp=temp.next;
//        temp.next=head.next;
//        return head.next;
//    }

}
