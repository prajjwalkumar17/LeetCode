package BinarySearchTree;

public class SearchInBST {
//    Basically as the tree is a BST we just need to **check wheather the root of tree contains the val or not** if not we compare the val with the root
//* if **val>root.val** then we traverse right side of tree
//* if **val<root.val** we travel left side of tree recursively
//* else if **root becomes null** that means key isn't there we return null
//
//    Hit upvote all hungrycoders out there if it fulfilled your hunger❤❤❤
//
//            ```
//    class Solution {
//        public TreeNode searchBST(TreeNode root, int val) {
//            if(root==null || root.val==val)return root;
//            if(val<root.val)return searchBST(root.left,val);
//            if(val>root.val)return searchBST(root.right,val);
//            return null;
//        }
//    }
//```
}
